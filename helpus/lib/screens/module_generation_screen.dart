import 'package:flutter/material.dart';

// Module generation screen
class ModuleGenerationScreen extends StatefulWidget {
  const ModuleGenerationScreen({Key? key}) : super(key: key);
  @override
  ModuleGenerationScreenState createState() => ModuleGenerationScreenState();
}

class ModuleGenerationScreenState extends State<ModuleGenerationScreen> {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Module Generation'),
    );
  }
}
